print in111;
for (@json a : @json_root recentMessage.messageList) {
    print in222;
    if (@json a.messageTypeCode == 18) {
        // sticker
        print in333;
        @str s = /static/wm/stickers/0/0/100/1/iPhone;
        @str s .. @json a.number;
        @str s .. .png;
        print @str s;
        @str s regex (/[^/]+\.png);
        print @str s;
    }
}
